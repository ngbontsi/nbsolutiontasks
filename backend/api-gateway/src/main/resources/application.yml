server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: ${AUTH_SERVICE_URI:http://localhost:8081}
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=1

        - id: restaurant-service
          uri: ${RESTAURANT_SERVICE_URI:http://localhost:8082}
          predicates:
            - Path=/api/restaurant/**
          filters:
            - StripPrefix=2

        - id: guesthouse-service
          uri: ${GUESTHOUSE_SERVICE_URI:http://localhost:8083}
          predicates:
            - Path=/api/guesthouse/**
          filters:
            - StripPrefix=2

        - id: marketplace-service
          uri: ${MARKETPLACE_SERVICE_URI:http://localhost:8084}
          predicates:
            - Path=/api/marketplace/**
          filters:
            - StripPrefix=2

        - id: task-api
          uri: ${TASK_API_URI:http://localhost:8085}
          predicates:
            - Path=/api/tasks/**

      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "https://ngbontsi.github.io"
              - "http://localhost:3000"
              - "http://localhost:5173"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"

jwt:
  secret: ${JWT_SECRET:YourSuperSecretKeyForJWTTokenGenerationAndValidation2024!}
  expiration: 86400000

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
